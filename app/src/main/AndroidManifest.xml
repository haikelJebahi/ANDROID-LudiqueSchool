<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ludiqueschool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LudiqueSchool">
        <activity
            android:name=".PageExercicesMaths">
        </activity>
        <activity
            android:name=".PageExercicesHistGeo" />
        <activity
            android:name=".PageChoixDifficulte"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".PageConnexion"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PageInscription"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".PageMenu" />

        <activity android:name=".PageResultat" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>